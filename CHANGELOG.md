# Changelog

用户可见的 Marketplace 版本变更。决策细节见 [`developer-record.md`](./developer-record.md)。  
版本号以 `package.json` 为准，由维护者发版时 bump；贡献者 PR 默认不改 version。  
某次发版当时捆的配置 schema / Skill 见 [`version/releases.json`](./version/releases.json)。

## Unreleased

已合入开发记录、待下次 Marketplace 发版（当前 `package.json` 仍为 **1.1.2**）：

- 嵌套 Git 仓库选择（父目录多仓库、路径映射、无仓库/无变更提示）
- 复制分组为 AI 上下文（路径 / 路径+内容）
- 一键工作集（打开的标签 / Git 变更）
- 配置导入 / 导出

## 1.1.2

- 见 Marketplace 已发布包；更早 MVP / 快捷键等能力见 [`README.md`](./README.md) 与 [`developer-record.md`](./developer-record.md)。
